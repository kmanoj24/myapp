<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Roleplay Chatbot</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f7f7f7;
      margin: 0;
      padding: 0;
    }
    .container {
      width: 80%;
      max-width: 800px;
      margin: 30px auto;
      background-color: white;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      border-radius: 8px;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    .form-container {
      margin-top: 20px;
    }
    .form-group {
      margin-bottom: 15px;
    }
    input, textarea {
      width: 100%;
      padding: 10px;
      margin: 5px 0;
      border-radius: 5px;
      border: 1px solid #ccc;
      font-size: 16px;
    }
    button {
      width: 100%;
      padding: 10px;
      background-color: #4CAF50;
      color: white;
      font-size: 18px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover {
      background-color: #45a049;
    }
    .chat-box {
      border: 1px solid #ccc;
      padding: 15px;
      height: 400px;
      overflow-y: scroll;
      margin-top: 20px;
      background-color: #f9f9f9;
      border-radius: 5px;
    }
    .chat-message {
      padding: 10px;
      margin-bottom: 10px;
      border-radius: 5px;
    }
    .user-message {
      background-color: #d1f7d6;
      align-self: flex-start;
    }
    .bot-message {
      background-color: #f0f0f0;
      align-self: flex-end;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Roleplay Chatbot</h1>

    <div class="form-container">
      <%= form_with url: ask_path, method: :post do |f| %>
        <div class="form-group">
          <%= f.label :character_name, "Character Name" %>
          <%= f.text_field :character_name, class: "form-control", placeholder: "Enter character name" %>
        </div>

        <div class="form-group">
          <%= f.label :situation, "Situation" %>
          <%= f.text_area :situation, class: "form-control", placeholder: "Describe the situation" %>
        </div>

        <div class="form-group">
          <%= f.label :user_prompt, "Your Message" %>
          <%= f.text_area :user_prompt, class: "form-control", placeholder: "What would you like to ask?" %>
        </div>

        <%= f.submit "Start Chat", class: "btn btn-primary" %>
      <% end %>
    </div>

    <% if @response %>
      <div class="chat-box">
        <div class="chat-message user-message">
          <strong>You:</strong>
          <p><%= params[:user_prompt] %></p>
        </div>
        <div class="chat-message bot-message">
          <strong><%= params[:character_name] %>:</strong>
          <p><%= @response %></p>
        </div>
      </div>
    <% end %>
  </div>
</body>
</html>
